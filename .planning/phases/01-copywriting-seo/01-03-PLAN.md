---
phase: 01-copywriting-seo
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
  - src/components/seo/SEOHead.tsx
  - src/pages/Home.tsx
  - src/pages/Services.tsx
  - src/pages/Portfolio.tsx
  - src/pages/About.tsx
  - src/pages/Contact.tsx
autonomous: false
requirements:
  - SEO-01
  - SEO-02
  - SEO-03
must_haves:
  truths:
    - "Un partage LinkedIn affiche un titre, une description et une image OG corrects — pas de contenu Lovable"
    - "Les meta tags title et description de toutes les pages decrivent le consulting automation, pas la photographie"
    - "Le structured data JSON-LD de type Person + ProfessionalService est present dans index.html"
    - "og:locale est fr_FR dans index.html et dans SEOHead"
  artifacts:
    - path: "index.html"
      provides: "Static fallback meta tags + OG tags + structured data JSON-LD"
      contains: "application/ld+json"
    - path: "src/components/seo/SEOHead.tsx"
      provides: "Dynamic meta tag manager with og:locale support"
      contains: "og:locale"
    - path: "public/images/og-image.jpg"
      provides: "OG image for social sharing"
  key_links:
    - from: "index.html"
      to: "schema.org"
      via: "JSON-LD script tag"
      pattern: "application/ld\\+json"
    - from: "src/components/seo/SEOHead.tsx"
      to: "src/data/business.ts"
      via: "businessInfo for defaults"
      pattern: "businessInfo"
    - from: "src/pages/Home.tsx"
      to: "src/data/business.ts"
      via: "businessInfo.seo.home"
      pattern: "businessInfo\\.seo\\.home"
---

<objective>
Implement complete SEO: update index.html static meta/OG tags, add structured data JSON-LD, enhance SEOHead with og:locale, update all pages to use businessInfo.seo for meta content, and create OG image.

Purpose: Make the site correctly indexed by search engines (automation consultant, not photographer) and shareable on LinkedIn with proper French OG tags. Eliminate all Lovable template remnants from meta tags.
Output: Properly SEO'd site with JSON-LD structured data, French OG tags, and page-specific meta descriptions.
</objective>

<execution_context>
@/Users/elias/Library/Mobile Documents/com~apple~CloudDocs/Business/Antigravity/Portfolio/.claude/get-shit-done/workflows/execute-plan.md
@/Users/elias/Library/Mobile Documents/com~apple~CloudDocs/Business/Antigravity/Portfolio/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-copywriting-seo/01-CONTEXT.md
@.planning/phases/01-copywriting-seo/01-RESEARCH.md
@.planning/phases/01-copywriting-seo/01-01-SUMMARY.md

<interfaces>
From src/data/business.ts (after Plan 01):
```typescript
// businessInfo.seo object created by Plan 01:
seo: {
  home: { title: string, description: string },
  about: { title: string, description: string },
  services: { title: string, description: string },
  portfolio: { title: string, description: string },
  contact: { title: string, description: string },
  ogImage: '/images/og-image.jpg',
}
```

From src/components/seo/SEOHead.tsx:
```typescript
interface SEOHeadProps {
  title?: string;
  description?: string;
  image?: string;
  type?: 'website' | 'article';
}
export function SEOHead(props: SEOHeadProps): null;
// Uses document.querySelector to update meta tags dynamically
// Already handles: description, og:title, og:description, og:type, og:url, og:image, og:site_name, twitter:card, twitter:title, twitter:description, author, keywords
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update index.html, SEOHead, create OG image, update all page meta tags</name>
  <files>index.html, src/components/seo/SEOHead.tsx, src/pages/Home.tsx, src/pages/Services.tsx, src/pages/Portfolio.tsx, src/pages/About.tsx, src/pages/Contact.tsx, public/images/og-image.jpg</files>
  <read_first>
    - index.html (current Lovable meta tags to replace)
    - src/components/seo/SEOHead.tsx (current implementation — understand updateMetaTag pattern)
    - src/data/business.ts (new seo fields from Plan 01)
    - src/pages/Home.tsx (current SEOHead usage)
    - src/pages/Services.tsx (current SEOHead usage)
    - src/pages/Portfolio.tsx (current SEOHead usage)
    - src/pages/About.tsx (after Plan 01 rewrite)
    - src/pages/Contact.tsx (current SEOHead usage — this file was modified by Plan 02)
    - src/assets/hero-portrait-real.jpg (exists — to copy as OG image)
    - .planning/phases/01-copywriting-seo/01-RESEARCH.md (Pattern 3 — index.html target, Pattern 5 — structured data)
  </read_first>
  <action>
This task modifies 7 files and creates 1 new file. Work through them in order:

**1. Create OG image:**
Copy `src/assets/hero-portrait-real.jpg` to `public/images/og-image.jpg`. Use CLI:
```bash
cp src/assets/hero-portrait-real.jpg public/images/og-image.jpg
```
This provides a real OG image for social sharing. LinkedIn will crop it — acceptable for Phase 1.

**2. Rewrite index.html completely:**
Replace the entire content with:
```html
<!doctype html>
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Elias — Automatisation sur mesure pour PME | n8n & Claude Code</title>
    <meta name="description" content="Je construis des automatisations sur mesure pour liberer du temps aux PME de services. Relances, onboarding, reporting — vos taches repetitives tournent sans vous." />
    <meta name="author" content="Elias" />
    <meta name="robots" content="index, follow" />

    <!-- OG / LinkedIn -->
    <meta property="og:title" content="Elias — Automatisation sur mesure pour PME" />
    <meta property="og:description" content="Je construis des automatisations sur mesure pour liberer du temps aux PME de services." />
    <meta property="og:type" content="website" />
    <meta property="og:image" content="/images/og-image.jpg" />
    <meta property="og:locale" content="fr_FR" />
    <meta property="og:site_name" content="Elias Automatisation" />

    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:title" content="Elias — Automatisation sur mesure pour PME" />
    <meta name="twitter:description" content="Je construis des automatisations sur mesure pour liberer du temps aux PME de services." />
    <meta name="twitter:image" content="/images/og-image.jpg" />

    <!-- Structured Data JSON-LD (SEO-03) -->
    <script type="application/ld+json">
    {
      "@context": "https://schema.org",
      "@type": ["Person", "ProfessionalService"],
      "name": "Elias",
      "jobTitle": "Consultant Automatisation PME",
      "description": "Automatisations sur mesure pour PME de services avec n8n et Claude Code.",
      "url": "https://elias-automation.com",
      "email": "contact@elias-automation.com",
      "address": {
        "@type": "PostalAddress",
        "addressCountry": "FR"
      },
      "areaServed": {
        "@type": "Country",
        "name": "France"
      },
      "knowsAbout": ["automatisation", "n8n", "Claude Code", "PME", "workflows"],
      "offers": {
        "@type": "Offer",
        "description": "Appel decouverte gratuit de 20 minutes",
        "price": "0",
        "priceCurrency": "EUR"
      }
    }
    </script>
  </head>

  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

Key changes from current:
- `lang="fr"` (was `lang="en"`)
- Real title and description (was "My App" / "A beautiful web application")
- Real OG tags (was Lovable branding)
- Added `og:locale` = `fr_FR`
- Added `robots` meta
- Removed `twitter:site` = `@lovable_dev`
- Added JSON-LD structured data with `["Person", "ProfessionalService"]`
- OG image points to `/images/og-image.jpg`

**3. Update SEOHead.tsx:**
Add `og:locale` to the useEffect meta tag updates. After the existing `updateMetaTag('og:site_name', ...)` line, add:
```typescript
updateMetaTag('og:locale', 'fr_FR', true);
```

Also update the default `image` prop to use the OG image:
```typescript
image = '/images/og-image.jpg',
```
So the OG image is always set (currently defaults to empty string which means no og:image update).

**4. Update Home.tsx SEOHead to use businessInfo.seo:**
Change:
```typescript
<SEOHead
  title={businessInfo.seo.home.title}
  description={businessInfo.seo.home.description}
  image={businessInfo.seo.ogImage}
/>
```

**5. Update Services.tsx SEOHead:**
Change the existing SEOHead call to:
```typescript
<SEOHead
  title={businessInfo.seo.services.title}
  description={businessInfo.seo.services.description}
  image={businessInfo.seo.ogImage}
/>
```
Services.tsx already imports businessInfo — no new import needed.

**6. Update Portfolio.tsx SEOHead:**
Change the existing SEOHead call to:
```typescript
<SEOHead
  title={businessInfo.seo.portfolio.title}
  description={businessInfo.seo.portfolio.description}
  image={businessInfo.seo.ogImage}
/>
```
Portfolio.tsx already imports businessInfo — no new import needed.

**7. Verify About.tsx SEOHead (already done in Plan 01):**
About.tsx should already use `businessInfo.seo.about.title` from Plan 01. Just verify — if it uses hardcoded strings, update to match pattern.

**8. Update Contact.tsx SEOHead:**
Contact.tsx was modified by Plan 02 (wave 1). Its SEOHead currently uses hardcoded strings. Update it to:
```typescript
<SEOHead
  title={businessInfo.seo.contact.title}
  description={businessInfo.seo.contact.description}
  image={businessInfo.seo.ogImage}
/>
```
Contact.tsx already imports businessInfo — no new import needed.
  </action>
  <verify>
    <automated>cd "/Users/elias/Library/Mobile Documents/com~apple~CloudDocs/Business/Antigravity/Portfolio" && npm run build 2>&1 | tail -5 && echo "---" && grep "og:locale" index.html && echo "---" && grep "application/ld+json" index.html && echo "---" && grep 'lang="fr"' index.html && echo "---" && test -f public/images/og-image.jpg && echo "OG image exists"</automated>
  </verify>
  <acceptance_criteria>
    - index.html contains `lang="fr"` (not `lang="en"`)
    - index.html does NOT contain `My App`
    - index.html does NOT contain `lovable.dev`
    - index.html does NOT contain `@lovable_dev`
    - index.html does NOT contain `A beautiful web application`
    - index.html contains `og:locale` with content `fr_FR`
    - index.html contains `application/ld+json`
    - index.html JSON-LD contains `"@type": ["Person", "ProfessionalService"]`
    - index.html contains `name="robots" content="index, follow"`
    - index.html contains `og:image` pointing to `/images/og-image.jpg`
    - SEOHead.tsx contains `og:locale` string
    - SEOHead.tsx default image prop is `/images/og-image.jpg` (not empty string)
    - Home.tsx contains `businessInfo.seo.home.title`
    - Services.tsx contains `businessInfo.seo.services.title`
    - Portfolio.tsx contains `businessInfo.seo.portfolio.title`
    - Contact.tsx contains `businessInfo.seo.contact.title`
    - File `public/images/og-image.jpg` exists
    - `npm run build` exits with code 0
  </acceptance_criteria>
  <done>All SEO implemented: index.html has French meta/OG tags + JSON-LD structured data, SEOHead sends og:locale, all pages use businessInfo.seo for meta content, OG image exists. No Lovable remnants.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify SEO and copy in browser</name>
  <files>n/a</files>
  <action>
Start the dev server with `npm run dev` and present the verification checklist to the user. This is a human-verify checkpoint — Claude starts the server, the user inspects the site visually.
  </action>
  <what-built>
    Complete copy rewrite (Plans 01+02) and SEO implementation (Plan 03):
    - All homepage sections have new B2B conversion copy from businessInfo
    - About page rewritten from photographer to automation consultant
    - Contact form with 4 qualifying fields in French
    - index.html has French meta/OG tags and JSON-LD structured data
    - All pages use businessInfo.seo for meta descriptions
  </what-built>
  <how-to-verify>
    1. Start dev server: `npm run dev`
    2. Visit http://localhost:5173/ — verify:
       - Hero headline is short (<10 words), clear, no jargon
       - No stats section visible (removed)
       - WhatIBuild shows 3 services with benefit-oriented descriptions
       - FeaturedProjects heading uses honest language (no fake client claims)
       - CTA section is inviting, mentions "appel decouverte"
    3. Visit http://localhost:5173/about — verify:
       - No mention of "Sarah Mitchell", "Photographer", "Visual Storyteller"
       - No Pexels video
       - Shows Elias portrait + honest consultant bio
       - LinkedIn link works
    4. Visit http://localhost:5173/contact — verify:
       - Form has 4 fields: prenom, email, secteur (dropdown), probleme (textarea)
       - Submit with empty fields shows French error messages
       - Submit with valid data triggers mailto and shows toast
    5. Right-click > View Source on any page — verify:
       - `<html lang="fr">`
       - No "My App" or "lovable" in meta tags
       - JSON-LD block visible with "Person" + "ProfessionalService"
    6. Open DevTools > Elements on each page — verify `<title>` changes per page
  </how-to-verify>
  <verify>
    <automated>cd "/Users/elias/Library/Mobile Documents/com~apple~CloudDocs/Business/Antigravity/Portfolio" && npm run build 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually verified the complete Phase 1 output: copy, form, SEO, and structured data all correct in browser.</done>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npm run build` passes
- `grep -c "lovable" index.html` returns 0
- `grep "og:locale" index.html` returns a match with fr_FR
- `grep "application/ld+json" index.html` returns a match
- `grep "lang=\"fr\"" index.html` returns a match
- All pages use `businessInfo.seo.*` for SEOHead props
</verification>

<success_criteria>
1. index.html has no Lovable remnants — title, description, OG tags all describe Elias automation consulting
2. JSON-LD structured data present with Person + ProfessionalService type
3. og:locale is fr_FR in both index.html and SEOHead
4. All pages (Home, About, Services, Portfolio, Contact) use businessInfo.seo for meta tags
5. OG image file exists at public/images/og-image.jpg
6. Human verification confirms everything looks correct in browser
</success_criteria>

<output>
After completion, create `.planning/phases/01-copywriting-seo/01-03-SUMMARY.md`
</output>
